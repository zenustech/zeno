#pragma once

#include "../../Structure.hpp"

namespace zeno {
    template<typename Equation,typename Pol,typename outTileVec>
    void pcg_with_fixed_solve(
        const Equation& A,
        outTileVec& vout,
        const zs::SmallString& outTag
    ) {
        
    }
};