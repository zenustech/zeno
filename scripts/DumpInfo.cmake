macro(do_dump_target_info target)
get_target_property(dirs ${target} INCLUDE_DIRECTORIES)
message("==================================")
message("INCLUDE_DIRECTORIES for ${target}")
message("----------------------------------")
foreach (dir ${dirs})
    message(${dir})
endforeach()
message("==================================")
endmacro()
